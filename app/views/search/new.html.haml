= form_tag search_show_path, method: :get do
  .text.small.flex-container.margin-m
    .flex-expanded.text.right
      genres:
    - @genres.each do |g|
      .margin-s-r
        = check_box_tag "search[genre][id][]", g.id
        = g.name

  .text.large.flex-container.baseline
    %div.flex-expanded
    %div
      = text_field_tag "search[content]", "", {class: "form", placeholder: "keyword here!"}
    %div.margin-m-l
      = button_tag "Submit", class: "btn", type: :submit
    %div.flex-expanded

- @discoveries.each do |d|
  = render "discoveries/item_discovery", discovery: d